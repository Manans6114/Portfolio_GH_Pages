# Assets

Place local images, icons, and other static imports here.

Import them directly in components, e.g.:

```jsx
import avatar from "../assets/avatar.png"
```

Files in `public/` (like `favicon.svg`) are served from the root path instead.
